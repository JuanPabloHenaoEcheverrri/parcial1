#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    float xo, yo, vxo, vyo, xoi, yoi;
    float xd, yd, vxd, vyd, xdi, ydi;
    int anguloo, angulod, t=0, d,vo, vd ;
    return 0;

    while(xo != xd || yo!=yd){
        vxo=vo*sin(anguloo*t);
        xo=xoi+vxo*t;
        t++;
         if (t>=2){
             if(sqrt(pow(xo,2)+pow(yo,2))-sqrt(pow(xdi,2)+pow(ydi,2))<=0.025*d){

             }


         }


    }
}
